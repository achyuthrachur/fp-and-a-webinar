---
phase: 09-webinar-readiness-and-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified: []
autonomous: false
requirements:
  - WBNR-01
  - WBNR-02
  - WBNR-03
  - WBNR-04

must_haves:
  truths:
    - "npm run build completes with zero TypeScript errors and zero warnings"
    - "The production URL shows zero console errors and zero React dev warnings in the browser DevTools console"
    - "All 6 preset scenarios load without NaN or error values on any KPI card or chart"
    - "Toggling dark/light theme while any preset is active shows all chart tick labels, axis text, and card text remaining readable in both themes"
    - "At 1920x1080 viewport the sticky header, all 6 section headings, Pipeline+AR side-by-side chart row, and 4-column KPI grid are visible with no overflow or whitespace gaps"
  artifacts:
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/.next"
      provides: "Production build output — existence proves zero TypeScript errors"
    - path: "Vercel production deployment URL"
      provides: "Live production surface for QA"
  key_links:
    - from: "Browser DevTools console"
      to: "Production URL"
      via: "Zero console errors confirms WBNR-03"
    - from: "Scenario Panel dropdown"
      to: "All KPI cards + all charts"
      via: "Each of 6 presets selected, values non-NaN"
---

<objective>
Run the production build, fix any TypeScript errors that surface, deploy to Vercel, then perform a structured browser QA checkpoint covering: layout at 1920px, dark/light theme correctness across all components, and all 6 scenario presets end-to-end.

Purpose: Final gate before the phase is complete — confirms everything built in Plans 01 and 02 works together as a cohesive presentation-ready dashboard.
Output: Clean production build, verified Vercel deployment, human sign-off on browser QA checklist.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-webinar-readiness-and-polish/09-CONTEXT.md
@.planning/phases/09-webinar-readiness-and-polish/09-RESEARCH.md
@.planning/phases/09-webinar-readiness-and-polish/09-01-SUMMARY.md
@.planning/phases/09-webinar-readiness-and-polish/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build check and Vercel deploy</name>
  <files></files>
  <action>
Run the full Vitest suite first to confirm no regressions from Plans 01 and 02, then run the TypeScript build check, then deploy to Vercel production.

**Step 1 — Vitest regression check (run from project root):**
```bash
node "Catie/FP&A Application/fpa-close-efficiency-dashboard/node_modules/vitest/vitest.mjs" run --reporter=verbose
```
Expected: all tests pass. If any test fails, read the failure and fix the root cause in the relevant source file before proceeding.

**Step 2 — TypeScript check (run from app directory):**
```bash
cd "Catie/FP&A Application/fpa-close-efficiency-dashboard" && npx tsc --noEmit 2>&1
```
Expected: no output (exit 0). If TypeScript errors surface:
- Missing imports from new components (DashboardHeader, SectionHeader) — fix the import path
- Type errors in new component files — fix the type annotation
- React fragment wrapper issues (missing `<>` or `</>`) — fix the JSX structure

Do NOT proceed to deploy if TypeScript exits non-zero.

**Step 3 — Vercel production deploy (run from app directory, NODE_TLS_REJECT_UNAUTHORIZED=0 required on Crowe network):**
```bash
cd "Catie/FP&A Application/fpa-close-efficiency-dashboard" && NODE_TLS_REJECT_UNAUTHORIZED=0 vercel deploy --prod --yes 2>&1 | tail -20
```
Expected: deployment URL printed at the end (e.g. `https://fpa-close-efficiency-dashboard-*.vercel.app` or the aliased production URL).

If Vercel build fails with a TypeScript or import error, read the build log output, fix the issue, and re-deploy.

**Step 4 — Note the production URL** for use in the browser QA checkpoint.
  </action>
  <verify>
    <automated>node "Catie/FP&A Application/fpa-close-efficiency-dashboard/node_modules/vitest/vitest.mjs" run --reporter=verbose</automated>
  </verify>
  <done>Vitest suite is green. `npx tsc --noEmit` exits 0. Vercel deploy succeeds and a production URL is available for browser QA.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 9 completes all four improvements:
1. Sticky `DashboardHeader` bar with Crowe wordmark, company/period label, and sun/moon theme toggle
2. `SectionHeader` component on all 6 sections (KPI Cards, Close Tracker, Margin Bridge, Pipeline/Charts, AI Summary, Scenario Controls)
3. `--muted-foreground` CSS alias — chart tick labels and other muted text are now visible in both themes
4. ChartsSection 2-column layout (Pipeline + AR side-by-side) confirmed — was already implemented in Phase 6

The production build is clean (zero TypeScript errors) and deployed to Vercel.
  </what-built>
  <how-to-verify>
Open the production Vercel URL from Task 1 in Chrome (or Edge) at 1920×1080 resolution if available, otherwise at your native resolution.

**A. Layout (WBNR-01)**
1. Confirm the sticky header bar is visible at the top of the main content column, showing "Crowe" on the left, company name + period in the center, and a sun/moon icon on the right
2. Scroll down in the main content — confirm the header stays pinned at the top while the content scrolls beneath it
3. Confirm the KPI section shows 8 cards in a 4-column × 2-row grid (no cards wrapping to a 5th column or collapsing to 2 columns)
4. Scroll to the Charts section — confirm Pipeline to Invoiced (left) and AR Aging (right) appear side-by-side in a two-column row, with 13-Week Cash Flow below spanning the full width
5. Confirm all 6 sections display a bold section label and a muted subtitle line beneath it (KPI Cards, Close Tracker, Margin Bridge, Pipeline & Collections Health, AI Executive Summary, Scenario Controls)

**B. Dark mode (WBNR-02)**
6. Click the moon/sun icon in the sticky header — confirm the entire dashboard switches to dark theme immediately
7. In dark mode, confirm all chart axis tick labels are visible (not invisible/white-on-white): check the Pipeline bar chart x-axis, AR Aging chart, Cash Flow line chart x-axis and y-axis, and Margin Bridge bar chart
8. Confirm KPI card values, Close Tracker stage text, and AI Summary text are all readable in dark mode
9. Click the toggle again — confirm switching back to light mode works and all text remains readable

**C. All 6 presets (WBNR-04)**
Using the Scenario Panel dropdown (left sidebar), load each preset one at a time. For each preset, confirm all 8 KPI cards show formatted currency values (not NaN, blank, or "undefined") and the Margin Bridge chart renders bars:

10. "Jan 2026 Baseline" — KPI cards show values, Margin Bridge renders
11. "Conservative Close" — KPI cards show values (EBITDA may be lower), Margin Bridge renders
12. "Q4 Push for Target" — KPI cards show values (Net Sales higher), Margin Bridge renders
13. "Fuel Cost Shock" — KPI cards show values (EBITDA stressed, may be negative but numeric), Margin Bridge renders
14. "Cash Preservation Mode" — KPI cards show values, Margin Bridge renders
15. "Optimistic Recovery" — KPI cards show values, Margin Bridge renders

**D. Console check (WBNR-03)**
16. Open DevTools → Console tab — confirm zero red errors and zero React warnings (yellow messages about missing deps or key props also count as failures)

Pass: All 16 items confirmed. Type "approved" to complete Phase 9.
Fail: Describe the specific item number and what you saw.
  </how-to-verify>
  <resume-signal>Type "approved" if all 16 items pass, or describe which item(s) failed and what you observed so the issue can be fixed.</resume-signal>
</task>

</tasks>

<verification>
Phase 9 is complete when:
1. Vitest suite: 86/86 green
2. `npx tsc --noEmit` exits 0
3. `vercel deploy --prod` succeeds with a live URL
4. Human QA sign-off on all 16 checklist items above
</verification>

<success_criteria>
- `npm run build` exits 0 (zero TypeScript errors)
- Production URL loads with zero console errors
- All 6 scenario presets produce valid (non-NaN) KPI values
- Dark mode: all chart tick labels, card text, and section headings readable
- Light mode: same
- Sticky header visible at all scroll positions
- All 6 section headers (title + subtitle) visible on scroll-through
- 4-column KPI grid and 2-column chart row confirmed at presentation resolution
</success_criteria>

<output>
After completion, create `.planning/phases/09-webinar-readiness-and-polish/09-03-SUMMARY.md` with standard summary format.

Also update `.planning/ROADMAP.md` Phase 9 entry:
- Mark status as Complete
- Update Plans count
- Add completion date 2026-03-05

Update `.planning/STATE.md`:
- Phase 9 complete
- All 9 phases complete
- Progress 100%
</output>
