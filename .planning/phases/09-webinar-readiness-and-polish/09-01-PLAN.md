---
phase: 09-webinar-readiness-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/app/globals.css
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/DashboardHeader.tsx
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/DashboardApp.tsx
autonomous: true
requirements:
  - WBNR-01
  - WBNR-02
  - WBNR-03

must_haves:
  truths:
    - "A sticky header bar appears at the top of the main content column showing 'Crowe' wordmark (left), company name + period (center), and a sun/moon theme toggle (right)"
    - "Clicking the theme toggle switches the dashboard between light and dark themes and persists to localStorage"
    - "Chart tick labels and Margin Bridge card secondary text are visible in both themes (--muted-foreground resolves to a real color)"
    - "The Vitest suite remains 86/86 green after all file edits"
  artifacts:
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/DashboardHeader.tsx"
      provides: "Sticky header component with Crowe wordmark, seedData context, and theme toggle"
      exports: ["DashboardHeader (default export)"]
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/app/globals.css"
      provides: "CSS variable --muted-foreground alias in both theme blocks"
      contains: "--muted-foreground: var(--muted-color)"
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/DashboardApp.tsx"
      provides: "DashboardHeader wired in place of slot-header div; footer text updated to Phase 9"
  key_links:
    - from: "DashboardApp.tsx"
      to: "DashboardHeader.tsx"
      via: "<DashboardHeader seedData={seedData} /> replaces <div id='slot-header' />"
    - from: "globals.css :root,html[data-theme='light'] block"
      to: "chart tick fill props"
      via: "--muted-foreground: var(--muted-color) alias"
    - from: "globals.css html[data-theme='dark'] block"
      to: "chart tick fill props"
      via: "--muted-foreground: var(--muted-color) alias"
---

<objective>
Fix the missing `--muted-foreground` CSS variable that makes chart tick labels invisible, create the sticky `DashboardHeader` component with Crowe wordmark and theme toggle, and wire it into `DashboardApp.tsx` in place of the placeholder div.

Purpose: Establishes the two foundational infrastructure changes — the CSS alias that fixes dark mode chart readability and the sticky header that anchors the webinar presentation layout.
Output: `DashboardHeader.tsx` (new), updated `globals.css` (alias added to both theme blocks), updated `DashboardApp.tsx` (header wired in).
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-webinar-readiness-and-polish/09-CONTEXT.md
@.planning/phases/09-webinar-readiness-and-polish/09-RESEARCH.md

<interfaces>
<!-- Key types and code contracts the executor needs. Extracted from codebase. No exploration required. -->

From src/components/DashboardApp.tsx (current state, lines 68-95):
```tsx
{/* Main content area: flex-1, scrollable */}
<main style={{ flex: 1, minWidth: 0, padding: '1.5rem', overflowY: 'auto' }}>
  <div id="slot-header" />   {/* ← REPLACE THIS with <DashboardHeader seedData={seedData} /> */}
  {seedData ? (
    <KpiSection seedData={seedData} />
  ) : (
    <div id="slot-kpi-section" />
  )}
  {seedData && <CloseTracker seedData={seedData} />}
  <MarginBridgeSection />
  {seedData && <ChartsSection seedData={seedData} />}
  {seedData && <AiSummarySection seedData={seedData} />}
  <p style={{ color: 'var(--foreground)', ... }}>
    FP&amp;A Close Efficiency Dashboard — Phase 8 AI Executive Summary active  {/* ← UPDATE to Phase 9 */}
  </p>
</main>
```

From src/lib/dataLoader.ts (DashboardSeedData interface, relevant fields):
```typescript
export interface DashboardSeedData {
  company: { name: string; closeTargetBusinessDays: number; };
  periodLabel: string;
  // ... other fields
}
```

From src/components/ui/icons.tsx (already exported):
```typescript
export { Sun1, Moon } from 'iconsax-react';  // Sun1 and Moon already exported
```

From src/app/globals.css (current state, showing both theme blocks):
```css
:root,
html[data-theme="light"] {
  --background: #f7f3ea;
  --foreground: #1c2d47;
  --card: #fffaf2cc;
  --surface: #fffdf8;
  --accent: #f5a800;
  --accent-soft: #ffd23126;
  --muted: #5e6b80;
  --border: #d7dce5;
  --track: #d7deea;
  --shadow: #011e41;
  --muted-color: #60728f;
  /* ADD HERE: --muted-foreground: var(--muted-color); */
}

html[data-theme="dark"] {
  --background: #0f1b2f;
  --foreground: #e7eef8;
  --card: #17263be0;
  --surface: #1f324f;
  --accent: #ffd231;
  --accent-soft: #ffd2311c;
  --muted: #abc0dd;
  --border: #2e4768;
  --track: #2a4464;
  --shadow: #000000;
  --muted-color: #9ab2d4;
  /* ADD HERE: --muted-foreground: var(--muted-color); */
}
```

From MarginBridgeSection.tsx (theme toggle pattern already battle-tested):
```typescript
const [isDark, setIsDark] = useState(
  typeof document !== 'undefined' &&
    document.documentElement.getAttribute('data-theme') === 'dark'
);
useEffect(() => {
  const observer = new MutationObserver(() => {
    setIsDark(document.documentElement.getAttribute('data-theme') === 'dark');
  });
  observer.observe(document.documentElement, {
    attributes: true,
    attributeFilter: ['data-theme'],
  });
  return () => observer.disconnect();
}, []);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --muted-foreground alias to globals.css</name>
  <files>Catie/FP&A Application/fpa-close-efficiency-dashboard/src/app/globals.css</files>
  <action>
Add `--muted-foreground: var(--muted-color);` as the last variable in BOTH theme blocks in globals.css.

Exact change — in the `:root, html[data-theme="light"]` block, after `--muted-color: #60728f;`, add:
```css
  --muted-foreground: var(--muted-color);
```

In the `html[data-theme="dark"]` block, after `--muted-color: #9ab2d4;`, add:
```css
  --muted-foreground: var(--muted-color);
```

This fixes: chart tick labels (PipelineChart, CashFlowChart, MarginBridgeChart all use `fill: 'var(--muted-foreground)'`), the CloseTracker h2 heading color, the MarginBridgeSection card header secondary text, and AiSummarySection empty state text. All were silently resolving to nothing.

Do NOT change any other part of globals.css.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const css=fs.readFileSync('Catie/FP&A Application/fpa-close-efficiency-dashboard/src/app/globals.css','utf8');const count=(css.match(/--muted-foreground: var\(--muted-color\)/g)||[]).length;if(count!==2)throw new Error('Expected 2 occurrences, got '+count);console.log('OK: --muted-foreground alias present in both theme blocks')"</automated>
  </verify>
  <done>globals.css contains `--muted-foreground: var(--muted-color);` in both the light and dark theme blocks (2 total occurrences).</done>
</task>

<task type="auto">
  <name>Task 2: Create DashboardHeader component and wire into DashboardApp</name>
  <files>
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/DashboardHeader.tsx
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/DashboardApp.tsx
  </files>
  <action>
**Step A — Create DashboardHeader.tsx:**

Create `src/components/dashboard/DashboardHeader.tsx` with NO `'use client'` directive (runs inside DashboardApp's existing client boundary).

Implement the component exactly as follows:

```tsx
// src/components/dashboard/DashboardHeader.tsx
// Sticky header bar for the main content column.
// No 'use client' — runs inside DashboardApp client boundary.
import { useState, useEffect, useCallback } from 'react';
import { Sun1, Moon } from '@/components/ui/icons';
import type { DashboardSeedData } from '@/lib/dataLoader';

interface DashboardHeaderProps {
  seedData: DashboardSeedData;
}

export default function DashboardHeader({ seedData }: DashboardHeaderProps) {
  const [isDark, setIsDark] = useState(
    typeof document !== 'undefined' &&
      document.documentElement.getAttribute('data-theme') === 'dark'
  );

  useEffect(() => {
    const observer = new MutationObserver(() => {
      setIsDark(document.documentElement.getAttribute('data-theme') === 'dark');
    });
    observer.observe(document.documentElement, {
      attributes: true,
      attributeFilter: ['data-theme'],
    });
    return () => observer.disconnect();
  }, []);

  const toggleTheme = useCallback(() => {
    const current = document.documentElement.getAttribute('data-theme');
    const next = current === 'dark' ? 'light' : 'dark';
    document.documentElement.setAttribute('data-theme', next);
    try { localStorage.setItem('theme', next); } catch (_) {}
  }, []);

  return (
    <header
      style={{
        position: 'sticky',
        top: 0,
        zIndex: 50,
        height: 56,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        padding: '0 1.25rem',
        background: isDark ? 'var(--card)' : '#ffffff',
        borderBottom: '1px solid var(--border)',
        // Cancel parent <main> padding (1.5rem) on left, right, and top edges
        marginLeft: '-1.5rem',
        marginRight: '-1.5rem',
        marginTop: '-1.5rem',
        width: 'calc(100% + 3rem)',
        marginBottom: '1.5rem',
      }}
    >
      {/* Left: Crowe wordmark */}
      <span
        style={{
          fontWeight: 700,
          fontSize: '1.125rem',
          color: isDark ? '#e7eef8' : '#011E41',
          fontFamily: '"Helvetica Now Display", "Helvetica Neue", Arial, sans-serif',
          letterSpacing: '-0.02em',
        }}
      >
        Crowe
      </span>

      {/* Center: company name + period label */}
      <span
        style={{
          color: 'var(--foreground)',
          fontSize: '0.875rem',
          fontWeight: 500,
        }}
      >
        {seedData.company.name} — {seedData.periodLabel}
      </span>

      {/* Right: theme toggle button */}
      <button
        onClick={toggleTheme}
        aria-label={isDark ? 'Switch to light theme' : 'Switch to dark theme'}
        style={{
          background: 'none',
          border: 'none',
          cursor: 'pointer',
          color: 'var(--muted)',
          padding: 4,
          display: 'flex',
          alignItems: 'center',
          borderRadius: 6,
        }}
      >
        {isDark
          ? <Sun1 size={20} color="var(--accent)" />
          : <Moon size={20} color="var(--foreground)" />
        }
      </button>
    </header>
  );
}
```

**Step B — Update DashboardApp.tsx:**

Make two targeted edits:

1. Add the DashboardHeader import after the existing AiSummarySection import line:
   ```tsx
   import DashboardHeader from '@/components/dashboard/DashboardHeader';
   ```

2. Replace `<div id="slot-header" />` (line 70) with:
   ```tsx
   {seedData && <DashboardHeader seedData={seedData} />}
   ```

3. Update the footer text (line 89) from:
   ```tsx
   FP&amp;A Close Efficiency Dashboard — Phase 8 AI Executive Summary active
   ```
   to:
   ```tsx
   FP&amp;A Close Efficiency Dashboard — Phase 9 Webinar Ready
   ```

Do NOT change any other part of DashboardApp.tsx.
  </action>
  <verify>
    <automated>node "Catie/FP&A Application/fpa-close-efficiency-dashboard/node_modules/vitest/vitest.mjs" run --reporter=verbose</automated>
  </verify>
  <done>DashboardHeader.tsx exists and exports a default function. DashboardApp.tsx imports it, replaces the slot-header div with `{seedData && &lt;DashboardHeader seedData={seedData} /&gt;}`, and the footer text says "Phase 9 Webinar Ready". Vitest suite remains 86/86 green.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `globals.css` has exactly 2 occurrences of `--muted-foreground: var(--muted-color)`
2. `DashboardHeader.tsx` exists at `src/components/dashboard/DashboardHeader.tsx`
3. `DashboardApp.tsx` imports `DashboardHeader` and renders `{seedData && <DashboardHeader seedData={seedData} />}` where the slot-header div was
4. Vitest suite: 86/86 green (no regressions)
5. TypeScript check: `cd "Catie/FP&A Application/fpa-close-efficiency-dashboard" && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- `--muted-foreground` CSS variable resolves in both light and dark themes
- Sticky header renders in the main content column with Crowe wordmark, company/period context, and working theme toggle
- Theme toggle persists selection to localStorage and updates `data-theme` on `<html>` immediately
- No TypeScript errors introduced
- Vitest suite 86/86 green
</success_criteria>

<output>
After completion, create `.planning/phases/09-webinar-readiness-and-polish/09-01-SUMMARY.md` with standard summary format.
</output>
