---
phase: 09-webinar-readiness-and-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/SectionHeader.tsx
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/KpiSection.tsx
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/CloseTracker/CloseTracker.tsx
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/MarginBridgeSection/MarginBridgeSection.tsx
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/ChartsSection/ChartsSection.tsx
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/AiSummarySection/AiSummarySection.tsx
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/ScenarioPanel/ScenarioPanel.tsx
autonomous: true
requirements:
  - WBNR-01
  - WBNR-02

must_haves:
  truths:
    - "All 6 sections display a two-line section heading: a bold uppercase label and a muted one-line subtitle immediately beneath it"
    - "Subtitle text is always visible — no hover or interaction required"
    - "Vitest suite remains 86/86 green after all edits"
  artifacts:
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/SectionHeader.tsx"
      provides: "Reusable two-line section heading component"
      exports: ["SectionHeader (default export)"]
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/KpiSection.tsx"
      provides: "SectionHeader added above KPI grid"
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/CloseTracker/CloseTracker.tsx"
      provides: "Existing h2 replaced by SectionHeader; subtitle added"
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/MarginBridgeSection/MarginBridgeSection.tsx"
      provides: "SectionHeader added above the card"
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/ChartsSection/ChartsSection.tsx"
      provides: "SectionHeader added above the chart row"
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/AiSummarySection/AiSummarySection.tsx"
      provides: "SectionHeader added above the card"
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/ScenarioPanel/ScenarioPanel.tsx"
      provides: "SectionHeader added at top of sidebar content"
  key_links:
    - from: "KpiSection.tsx"
      to: "SectionHeader.tsx"
      via: "import and render above <section> grid"
    - from: "CloseTracker.tsx"
      to: "SectionHeader.tsx"
      via: "replaces existing <h2>Month-End Close Tracker</h2>"
    - from: "MarginBridgeSection.tsx"
      to: "SectionHeader.tsx"
      via: "rendered above the card <div> in return JSX"
    - from: "ChartsSection.tsx"
      to: "SectionHeader.tsx"
      via: "rendered as first child of outer column flex container"
    - from: "AiSummarySection.tsx"
      to: "SectionHeader.tsx"
      via: "rendered above the <section> card element"
    - from: "ScenarioPanel.tsx"
      to: "SectionHeader.tsx"
      via: "rendered as first child of outer padding div, before PresetRow"
---

<objective>
Create the reusable `SectionHeader` component and add it to all 6 dashboard sections (KPI Cards, Close Tracker, Margin Bridge, Charts, AI Summary, Scenario Panel) using the exact subtitle text locked in CONTEXT.md.

Purpose: Gives every dashboard section a consistent two-line heading that establishes context for a CFO-level audience watching the webinar.
Output: `SectionHeader.tsx` (new), 6 existing component files updated with section headings.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-webinar-readiness-and-polish/09-CONTEXT.md
@.planning/phases/09-webinar-readiness-and-polish/09-RESEARCH.md

<interfaces>
<!-- Exact subtitle text locked in CONTEXT.md. Use verbatim — do not paraphrase. -->

Section heading inventory:

| Component File | Title | Subtitle |
|---------------|-------|----------|
| KpiSection.tsx | "KPI Cards" | "January 2026 Performance Snapshot — Key financials against prior month and scenario adjustments" |
| CloseTracker/CloseTracker.tsx | "Close Tracker" | "Month-End Close Progress — Journal entry completion rates and days remaining to close target" |
| MarginBridgeSection/MarginBridgeSection.tsx | "Margin Bridge" | "Scenario Impact on EBITDA — How lever adjustments flow through revenue, margin, and cost to adjusted EBITDA" |
| ChartsSection/ChartsSection.tsx | "Pipeline & Collections Health" | "AR aging risk, CRM pipeline funnel, and 13-week cash outlook" |
| AiSummarySection/AiSummarySection.tsx | "AI Executive Summary" | "AI-Generated CFO Narrative — Two-paragraph executive summary synthesized from current scenario KPIs" |
| ScenarioPanel/ScenarioPanel.tsx | "Scenario Controls" | "Adjust revenue, cost, and operations levers to model close outcomes in real time" |

Current structure of CloseTracker.tsx (the h2 to REPLACE):
```tsx
export function CloseTracker({ seedData }: CloseTrackerProps) {
  return (
    <section style={{ marginBottom: '2rem' }}>
      <h2
        style={{
          fontSize: '0.75rem',
          fontWeight: 700,
          color: 'var(--muted-foreground)',
          marginBottom: '0.75rem',
          textTransform: 'uppercase',
          letterSpacing: '0.08em',
        }}
      >
        Month-End Close Tracker
      </h2>
      <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem' }}>
```
→ Replace the entire <h2> element with <SectionHeader title="Close Tracker" subtitle="..." />

Current structure of KpiSection.tsx (insert SectionHeader BEFORE the <section> grid):
```tsx
export default function KpiSection({ seedData }: KpiSectionProps) {
  // ...useSelector calls...
  return (
    <section aria-label="KPI Metrics" style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', ... }}>
      {/* cards */}
    </section>
  );
}
```
→ Wrap in a React fragment and add SectionHeader before the <section>.

Current structure of MarginBridgeSection.tsx (insert SectionHeader BEFORE the card div):
```tsx
return (
  <div ref={cardRef} style={{ background: 'var(--card)', borderRadius: 12, ... }}>
    {/* Card header */}
    <div>Margin Bridge ...</div>
    {/* Chart */}
  </div>
);
```
→ Wrap in a React fragment and add SectionHeader before the outer <div ref={cardRef}>.

Current structure of ChartsSection.tsx (insert SectionHeader INSIDE the outer column div, as first child):
```tsx
return (
  <div style={{ display: 'flex', flexDirection: 'column', gap: '1.5rem', marginTop: '1.5rem' }}>
    <div style={{ display: 'flex', gap: '1.5rem' }}>  {/* Pipeline + AR row */}
      ...
    </div>
    <CashFlowChart />
  </div>
);
```
→ Add SectionHeader as first child of outer column div.

Current structure of AiSummarySection.tsx (insert SectionHeader BEFORE the <section> card):
```tsx
return (
  <section style={{ marginTop: '1.5rem', background: 'var(--card)', ... }}>
    {/* Card header */}
    ...
  </section>
);
```
→ Wrap in a React fragment and add SectionHeader before the <section>.

Current structure of ScenarioPanel.tsx return (insert SectionHeader as first child of outer div, before the <style> tag):
```tsx
return (
  <div style={{ padding: '1rem 0.875rem', display: 'flex', flexDirection: 'column', gap: '1.25rem' }}>
    {/* Switch thumb CSS */}
    <style>{`...`}</style>
    {/* PresetRow */}
    <PresetRow ... />
    ...
  </div>
);
```
→ Add SectionHeader as first child of the outer div (before the <style> block).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SectionHeader component</name>
  <files>Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/SectionHeader.tsx</files>
  <action>
Create `src/components/dashboard/SectionHeader.tsx`. No `'use client'` directive. Implement exactly:

```tsx
// src/components/dashboard/SectionHeader.tsx
// Reusable two-line section heading used across all 6 dashboard sections.
// No 'use client' — runs inside DashboardApp client boundary.

interface SectionHeaderProps {
  title: string;
  subtitle: string;
}

export default function SectionHeader({ title, subtitle }: SectionHeaderProps) {
  return (
    <div style={{ marginBottom: '0.75rem' }}>
      <h2
        style={{
          fontSize: '0.75rem',
          fontWeight: 700,
          color: 'var(--muted)',
          textTransform: 'uppercase',
          letterSpacing: '0.08em',
          margin: 0,
        }}
      >
        {title}
      </h2>
      <p
        style={{
          fontSize: '0.8125rem',
          color: 'var(--muted-color)',
          margin: '0.125rem 0 0',
          lineHeight: 1.4,
        }}
      >
        {subtitle}
      </p>
    </div>
  );
}
```

Note: Uses `var(--muted)` for the title (confirmed present in globals.css) and `var(--muted-color)` for subtitle (also confirmed present). Does NOT use `var(--muted-foreground)` directly — that alias is added in Plan 09-01. The choice of `--muted` vs `--muted-color` here is intentional: title uses the stronger muted color, subtitle uses the secondary muted color.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const src=fs.readFileSync('Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/SectionHeader.tsx','utf8');if(!src.includes('export default function SectionHeader'))throw new Error('SectionHeader not exported');if(src.includes('use client'))throw new Error('Should not have use client');console.log('OK: SectionHeader.tsx valid')"</automated>
  </verify>
  <done>SectionHeader.tsx exists, exports a default function accepting `{ title, subtitle }` props, has no `'use client'` directive, and renders a two-line heading using CSS variables.</done>
</task>

<task type="auto">
  <name>Task 2: Add SectionHeader to all 6 sections</name>
  <files>
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/KpiSection.tsx
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/CloseTracker/CloseTracker.tsx
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/MarginBridgeSection/MarginBridgeSection.tsx
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/ChartsSection/ChartsSection.tsx
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/AiSummarySection/AiSummarySection.tsx
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/ScenarioPanel/ScenarioPanel.tsx
  </files>
  <action>
Apply the following targeted edits to each file. Do not change any logic, state, or existing styling — only add the SectionHeader import and render call.

**KpiSection.tsx:**
- Add import: `import SectionHeader from '@/components/dashboard/SectionHeader';`
- Wrap the return value in a React fragment `<>...</>` and add SectionHeader before the `<section>` grid:
  ```tsx
  return (
    <>
      <SectionHeader
        title="KPI Cards"
        subtitle="January 2026 Performance Snapshot — Key financials against prior month and scenario adjustments"
      />
      <section aria-label="KPI Metrics" style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: '1rem', padding: '0 0 1.5rem 0' }}>
        {/* existing KpiCard elements unchanged */}
      </section>
    </>
  );
  ```

**CloseTracker/CloseTracker.tsx:**
- Add import: `import SectionHeader from '@/components/dashboard/SectionHeader';`
- Remove the entire existing `<h2>` element (lines with `Month-End Close Tracker` text).
- Add SectionHeader in its place:
  ```tsx
  <SectionHeader
    title="Close Tracker"
    subtitle="Month-End Close Progress — Journal entry completion rates and days remaining to close target"
  />
  ```
  The outer `<section>` element and the inner `<div>` with `DaysToCloseCard` and `StageCard` elements remain unchanged.

**MarginBridgeSection/MarginBridgeSection.tsx:**
- Add import: `import SectionHeader from '@/components/dashboard/SectionHeader';`
- Wrap the return value in a React fragment `<>...</>` and add SectionHeader before the `<div ref={cardRef}>` card:
  ```tsx
  return (
    <>
      <SectionHeader
        title="Margin Bridge"
        subtitle="Scenario Impact on EBITDA — How lever adjustments flow through revenue, margin, and cost to adjusted EBITDA"
      />
      <div ref={cardRef} style={{ background: 'var(--card)', ... }}>
        {/* existing card header and chart unchanged */}
      </div>
    </>
  );
  ```

**ChartsSection/ChartsSection.tsx:**
- Add import: `import SectionHeader from '@/components/dashboard/SectionHeader';`
- Add SectionHeader as the FIRST child of the outer column flex div (before the inner row div):
  ```tsx
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: '1.5rem', marginTop: '1.5rem' }}>
      <SectionHeader
        title="Pipeline & Collections Health"
        subtitle="AR aging risk, CRM pipeline funnel, and 13-week cash outlook"
      />
      <div style={{ display: 'flex', gap: '1.5rem' }}>
        {/* Pipeline + AR row — unchanged */}
      </div>
      <CashFlowChart data={seedData.cash13Week} />
    </div>
  );
  ```

**AiSummarySection/AiSummarySection.tsx:**
- Add import: `import SectionHeader from '@/components/dashboard/SectionHeader';`
- Wrap the return value in a React fragment `<>...</>` and add SectionHeader before the `<section>` card:
  ```tsx
  return (
    <>
      <SectionHeader
        title="AI Executive Summary"
        subtitle="AI-Generated CFO Narrative — Two-paragraph executive summary synthesized from current scenario KPIs"
      />
      <section style={{ marginTop: '1.5rem', background: 'var(--card)', ... }}>
        {/* existing card header, body, streaming content unchanged */}
      </section>
    </>
  );
  ```

**ScenarioPanel/ScenarioPanel.tsx:**
- Add import: `import SectionHeader from '@/components/dashboard/SectionHeader';`
- Add SectionHeader as the FIRST child of the outer `<div>` in the `ScenarioPanel` function return (before the `<style>` block and `<PresetRow>`):
  ```tsx
  return (
    <div style={{ padding: '1rem 0.875rem', display: 'flex', flexDirection: 'column', gap: '1.25rem' }}>
      <SectionHeader
        title="Scenario Controls"
        subtitle="Adjust revenue, cost, and operations levers to model close outcomes in real time"
      />
      {/* Switch thumb CSS — unchanged */}
      <style>{`...`}</style>
      {/* PresetRow and control groups — unchanged */}
    </div>
  );
  ```

After all edits, run the Vitest suite to confirm no regressions.
  </action>
  <verify>
    <automated>node "Catie/FP&A Application/fpa-close-efficiency-dashboard/node_modules/vitest/vitest.mjs" run --reporter=verbose</automated>
  </verify>
  <done>All 6 component files import SectionHeader and render it with the exact title and subtitle text from CONTEXT.md. Vitest suite remains 86/86 green. TypeScript check passes: `cd "Catie/FP&A Application/fpa-close-efficiency-dashboard" && npx tsc --noEmit` exits 0.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `SectionHeader.tsx` exists and exports a default function
2. All 6 sections have `import SectionHeader` and a `<SectionHeader title="..." subtitle="..." />` render call
3. CloseTracker no longer has the old `<h2>Month-End Close Tracker</h2>` element
4. Subtitle text matches CONTEXT.md exactly (verbatim, not paraphrased)
5. Vitest suite: 86/86 green
6. `npx tsc --noEmit` exits 0 from inside the app directory
</verification>

<success_criteria>
- SectionHeader renders a bold uppercase title and a muted one-line subtitle in a `0.75rem` div
- All 6 sections display their section header (visible on scroll, no interaction needed)
- No existing component logic, styles, or data flows are changed
- Vitest suite green, TypeScript clean
</success_criteria>

<output>
After completion, create `.planning/phases/09-webinar-readiness-and-polish/09-02-SUMMARY.md` with standard summary format.
</output>
