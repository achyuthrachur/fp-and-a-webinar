---
phase: 10-visual-identity-and-interactivity
plan: 06
type: execute
wave: 4
depends_on: [10-02, 10-03, 10-04, 10-05]
files_modified: []
autonomous: false
requirements: [VISU-01, VISU-02, VISU-03, VISU-04]

must_haves:
  truths:
    - "Landing page at / shows Crowe Indigo Dark background with React Bits animation, wordmark, headline, 3 feature bullets, Amber CTA"
    - "Dashboard at /dashboard shows existing full-featured dashboard — all scenario/chart/AI functionality intact"
    - "At least 3 sections (all 6 in practice) animate with fade+slide-up on first scroll into view"
    - "KPI cards stagger in with ~60ms delay per card"
    - "Explain button in DashboardHeader toggles all 6 explanation panels simultaneously"
    - "Explanation panels show correct locked text, animate open/close smoothly"
    - "explainMode persists across page refresh (localStorage)"
    - "shadcn Button used for Generate/Regenerate and Reset"
    - "shadcn Select used for preset dropdown (all 6 presets work)"
    - "shadcn Tooltip shows on KPI card label hover"
    - "Production build passes: npm run build completes with zero TypeScript errors"
    - "Zero browser console errors on both / and /dashboard"
  artifacts:
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/app/page.tsx"
      provides: "Landing page — verified in browser"
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/app/dashboard/page.tsx"
      provides: "Dashboard route — verified in browser"
  key_links:
    - from: "/"
      to: "/dashboard"
      via: "CTA button click"
      pattern: "href=/dashboard"
---

<objective>
Deploy to Vercel and run the 19-point browser QA checklist across all 4 VISU requirements. This is the final gate before Phase 10 is marked complete.

Purpose: Phase 10 has no automated tests for visual/interactive behavior — browser QA is the only verification method. This checkpoint confirms all 4 requirements are met before marking the phase complete.
Output: Human verification of all Phase 10 deliverables at production URL.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-visual-identity-and-interactivity/10-CONTEXT.md
@.planning/phases/10-visual-identity-and-interactivity/10-VALIDATION.md
@.planning/phases/10-visual-identity-and-interactivity/10-04-SUMMARY.md
@.planning/phases/10-visual-identity-and-interactivity/10-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build check and Vercel deployment</name>
  <files></files>
  <action>
From the project root, run the full Vitest suite one final time to confirm all phases of work are stable:

```bash
node "Catie/FP&A Application/fpa-close-efficiency-dashboard/node_modules/vitest/vitest.mjs" run --reporter=verbose
```
Expected: 86/86 GREEN.

Then run TypeScript check:
```bash
cd "Catie/FP&A Application/fpa-close-efficiency-dashboard" && npx tsc --noEmit
```
Expected: 0 errors.

Then build:
```bash
cd "Catie/FP&A Application/fpa-close-efficiency-dashboard" && npm run build
```
Expected: Completes with no errors. If TypeScript errors appear during build, fix them before proceeding to deployment.

If build passes, commit Phase 10 changes to git:
```bash
git add "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/" "Catie/FP&A Application/fpa-close-efficiency-dashboard/package.json" "Catie/FP&A Application/fpa-close-efficiency-dashboard/package-lock.json"
git commit -m "feat(phase-10): visual identity and interactivity — landing page, animations, shadcn components, explain mode"
```

Deploy to Vercel:
```bash
cd "Catie/FP&A Application/fpa-close-efficiency-dashboard" && NODE_TLS_REJECT_UNAUTHORIZED=0 vercel deploy --prod --yes
```

Note the production URL from the deploy output. This is the URL used for browser QA in the checkpoint task.
  </action>
  <verify>
    <automated>node "Catie/FP&A Application/fpa-close-efficiency-dashboard/node_modules/vitest/vitest.mjs" run --reporter=verbose</automated>
  </verify>
  <done>Vitest 86/86 GREEN. TypeScript clean. `npm run build` passes. Vercel deployment succeeds with a production URL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 10 complete implementation across all 4 VISU requirements:
- VISU-01: Landing page at / (Crowe Indigo Dark, React Bits particles, Amber CTA to /dashboard)
- VISU-02: Framer Motion whileInView animations on all 6 dashboard sections + staggered KPI cards
- VISU-03: shadcn Button (Generate/Regenerate/Reset), shadcn Select (preset dropdown), shadcn Tooltip (KPI cards)
- VISU-04: Explain mode button in DashboardHeader, 6 explanation panels with locked text, localStorage persistence
  </what-built>
  <how-to-verify>
Open the Vercel production URL in Chrome at 1920x1080 (or closest available). Check each item:

**VISU-01 — Landing Page**
1. Visit `/` — confirm: full-screen Crowe Indigo Dark (#011E41) background, animated particles (no console errors), "Crowe" wordmark, "FP&A Close Efficiency Dashboard" headline, "Summit Logistics Group · January 2026" subheadline, 3 feature bullets with icons, amber "Enter Dashboard →" button
2. Click "Enter Dashboard →" — confirm: URL changes to `/dashboard`, existing dashboard loads correctly
3. Visit `/dashboard` directly — confirm: dashboard loads with all functionality (sliders, charts, AI summary)

**VISU-02 — Animations**
4. At `/dashboard`, load fresh (Ctrl+Shift+R) — watch KPI section: 8 cards should appear left-to-right with slight stagger (~60ms each)
5. Scroll down slowly — each section (Close Tracker, Margin Bridge, Charts, AI Summary) should fade+slide-up as it enters viewport
6. Scroll back up past a section, then back down — animations should NOT re-fire (once: true)
7. Open Chrome DevTools > Console > type: `window.matchMedia('(prefers-reduced-motion: reduce)').matches` — if true, no animations should play. (If false, set OS to reduce motion to test, or visually confirm animations are working for the non-reduced-motion case)

**VISU-03 — shadcn Components**
8. In AI Summary section: confirm "Generate Summary" / "Regenerate" button has Crowe amber background, rounded corners, proper hover state
9. In Scenario Panel sidebar: open the preset dropdown — confirm it shows all 6 named presets, Crowe-themed styling, Amber highlight on hover
10. Select any non-baseline preset — confirm KPI values update correctly (Redux logic intact)
11. Click "Reset to defaults" — confirm all controls reset
12. Hover over a KPI card label (e.g., "Net Sales") — confirm Tooltip appears with description text
13. Toggle to dark theme — confirm shadcn Button, Select, and Tooltip all render correctly in dark mode (no invisible text, no broken styling)

**VISU-04 — Explain Mode**
14. In DashboardHeader (top right area) — confirm "Explain" button appears to the left of the moon/sun theme toggle
15. Click "Explain" — confirm: all 6 sections simultaneously reveal explanation panels with smooth animation; button label changes to "Hide Explanations"
16. Verify explanation panel for KPI Cards reads: "These 8 metrics reflect Summit Logistics Group's January 2026 GL data, adjusted in real time for the active scenario..."
17. Verify explanation panel for Close Tracker reads: "Progress bars are computed from actual journal entry counts in erp_journal_entries.csv..."
18. Click "Hide Explanations" — confirm all 6 panels collapse smoothly
19. Refresh page with Explain mode ON — confirm panels are still visible after refresh (localStorage persistence)

**Final checks**
- Open DevTools Console: confirm zero errors on `/` and `/dashboard`
- Confirm all existing scenario sliders, AI summary generation, and dark mode toggle still work correctly
  </how-to-verify>
  <resume-signal>
Type "approved" if all 19 checks pass. If any check fails, describe which items failed and what was observed — Claude will create a gap closure plan.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 10 complete when all 19 browser QA items pass:
1. Landing page renders at /
2. CTA navigates to /dashboard
3. Dashboard at /dashboard intact
4. KPI card stagger animation on load
5. Section scroll animations fire on first view
6. Animations do NOT re-fire on second scroll
7. prefers-reduced-motion respected
8. shadcn Button in AI Summary
9. shadcn Select preset dropdown works
10. Preset selection updates KPIs
11. Reset button works
12. KPI Tooltip on hover
13. Dark mode shadcn rendering
14. Explain button in header
15. All 6 explanation panels appear
16. KPI Cards explanation text matches exactly
17. Close Tracker explanation text matches exactly
18. Hide Explanations collapses panels
19. explainMode localStorage persistence
</verification>

<success_criteria>
All 4 VISU requirements verified by human QA:
- VISU-01: Landing splash screen at / with CTA to /dashboard
- VISU-02: At least 3 (all 6) sections with entrance animations on scroll
- VISU-03: shadcn components for primary interactive controls
- VISU-04: 6 contextual explanation panels with show/hide toggle
Phase 10 marked complete. Milestone v1.0 complete (all 10 phases done).
</success_criteria>

<output>
After human approval, create `.planning/phases/10-visual-identity-and-interactivity/10-06-SUMMARY.md`
</output>
