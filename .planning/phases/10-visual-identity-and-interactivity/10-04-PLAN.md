---
phase: 10-visual-identity-and-interactivity
plan: 04
type: execute
wave: 3
depends_on: [10-01]
files_modified:
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/ui/Button.tsx
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/ui/Select.tsx
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/ui/Tooltip.tsx
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/AiSummarySection/AiSummarySection.tsx
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/ScenarioPanel/ScenarioPanel.tsx
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/KpiCard.tsx
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/app/globals.css
autonomous: true
requirements: [VISU-03]

must_haves:
  truths:
    - "Generate/Regenerate button in AiSummarySection uses shadcn Button component (cn() + Crowe amber styling)"
    - "Reset to defaults button in ScenarioPanel uses shadcn Button component"
    - "Preset dropdown in ScenarioPanel uses shadcn Select component (all 6 presets still load, sentinel option works)"
    - "KPI cards show a shadcn Tooltip on the icon/label area with a brief description"
    - "All components render correctly in both light and dark themes"
    - "All 86 Vitest tests remain GREEN — no regressions in Redux or selector logic"
  artifacts:
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/ui/Button.tsx"
      provides: "shadcn-style Button using cn() and Crowe CSS variables"
      exports: ["Button"]
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/ui/Select.tsx"
      provides: "shadcn-style Select wrapping @radix-ui/react-select with Crowe theming"
      exports: ["Select", "SelectTrigger", "SelectContent", "SelectItem", "SelectValue"]
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/ui/Tooltip.tsx"
      provides: "shadcn-style Tooltip wrapping @radix-ui/react-tooltip with Crowe theming"
      exports: ["Tooltip", "TooltipTrigger", "TooltipContent", "TooltipProvider"]
  key_links:
    - from: "src/components/dashboard/AiSummarySection/AiSummarySection.tsx"
      to: "src/components/ui/Button.tsx"
      via: "import { Button } from '@/components/ui/Button'"
      pattern: "Button.*handleGenerate"
    - from: "src/components/dashboard/ScenarioPanel/ScenarioPanel.tsx"
      to: "src/components/ui/Select.tsx"
      via: "import { Select, ... } from '@/components/ui/Select'"
      pattern: "SelectPrimitive|Select.*onValueChange"
    - from: "src/components/dashboard/KpiCard.tsx"
      to: "src/components/ui/Tooltip.tsx"
      via: "import { Tooltip, ... } from '@/components/ui/Tooltip'"
      pattern: "TooltipContent"
---

<objective>
Create shadcn-style Button, Select, and Tooltip components via copy-paste (no CLI), then replace hand-crafted buttons, dropdown, and KPI card hover hints with these components.

Purpose: VISU-03 — shadcn/ui components replace hand-crafted primitives. Copy-paste model required because shadcn CLI init conflicts with Tailwind v4 (REQUIREMENTS.md line 107).
Output: 3 new ui components + 4 updated section components + globals.css additive shadcn tokens.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-visual-identity-and-interactivity/10-CONTEXT.md
@.planning/phases/10-visual-identity-and-interactivity/10-RESEARCH.md
@.planning/phases/10-visual-identity-and-interactivity/10-01-SUMMARY.md

<interfaces>
<!-- Already installed Radix UI primitives (from package.json) -->
```
@radix-ui/react-select: ^2.1.0
@radix-ui/react-tooltip: ^1.1.0
@radix-ui/react-slider: ^1.2.0
@radix-ui/react-switch: ^1.1.0
```

<!-- cn() utility (created in Plan 10-01) -->
```typescript
// src/lib/utils.ts
export function cn(...inputs: ClassValue[]): string
```

<!-- Existing globals.css CSS variables (MUST NOT be overwritten) -->
```css
--background, --foreground, --card, --surface, --accent, --accent-soft,
--muted, --border, --track, --shadow, --muted-color, --muted-foreground
```
<!-- These are the existing dashboard variables used by 10+ files. -->
<!-- shadcn variables must be ADDITIVE — add --primary, --primary-foreground, --ring, --radius, --input, --popover, --popover-foreground, --destructive alongside existing ones -->

<!-- CRITICAL: AiSummarySection current button (to be replaced) -->
```typescript
<button
  onClick={handleGenerate}
  disabled={isStreaming}
  style={{ padding: '0.375rem 0.875rem', borderRadius: '6px', border: 'none',
           background: isStreaming ? 'var(--muted)' : 'var(--accent)',
           color: isStreaming ? 'var(--muted-foreground)' : '#011E41',
           fontWeight: 600, fontSize: '0.8125rem',
           cursor: isStreaming ? 'not-allowed' : 'pointer',
           opacity: isStreaming ? 0.6 : 1,
           transition: 'opacity 150ms ease, background 150ms ease' }}
>
  {isStreaming ? 'Generating...' : buttonLabel}
</button>
```

<!-- CRITICAL: ScenarioPanel uses SelectPrimitive directly (to be replaced) -->
```typescript
import * as SelectPrimitive from '@radix-ui/react-select';
// Used as <SelectPrimitive.Root>, <SelectPrimitive.Trigger>, <SelectPrimitive.Content>, etc.
// Custom sentinel option: value="__custom__" for partial edits showing "— Custom —"
// onValueChange handler dispatches loadPreset or handles __custom__ sentinel
```

<!-- KpiCard.tsx — needs Tooltip on the label area (currently no tooltip) -->
// KpiCard renders label, icon, value, and delta. Add a Tooltip around the label.
// Brief tooltip text per card: e.g., "Net Sales: Revenue after returns and discounts"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shadcn Button, Select, and Tooltip components + add shadcn CSS variables to globals.css</name>
  <files>
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/ui/Button.tsx
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/ui/Select.tsx
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/ui/Tooltip.tsx
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/app/globals.css
  </files>
  <action>
Create 3 new UI component files and extend globals.css. All components use cn() for class merging and reference existing Crowe CSS variables directly (not shadcn's default --primary etc.). This avoids variable naming conflicts.

**globals.css: Add shadcn compatibility tokens (ADDITIVE — append after existing variables)**

Add these at the end of each theme block in globals.css. These are NEW variable names not currently used — they do NOT replace any existing variables:

```css
/* Add inside :root, html[data-theme="light"] block */
--radius: 0.625rem;
--primary: 1 30 65;             /* Crowe Indigo Dark as HSL channel values (for shadcn HSL format) */
--primary-foreground: 246 247 250;  /* soft white */
--ring: 0 46 98;                /* Crowe Indigo Core */
--input: 215 17% 91%;           /* maps to --border value space */
--popover: 255 255 255;         /* pure white for popovers */
--popover-foreground: 228 20% 22%;

/* Add inside html[data-theme="dark"] block */
--radius: 0.625rem;
--primary: 231 244 248;
--primary-foreground: 1 30 65;
--ring: 0 63 159;
--input: 228 20% 25%;
--popover: 23 38 59;
--popover-foreground: 231 244 248;
```

NOTE: In practice, shadcn HSL variables use a non-standard `H S% L%` format. Since our Button/Select/Tooltip components will use existing Crowe CSS variables directly (var(--accent), var(--foreground), var(--border), var(--card), var(--surface)), we don't actually NEED the shadcn HSL tokens. Add --radius only (used by rounded-* in component class names). Skip the other shadcn tokens to avoid any risk of conflict.

Simpler approach — add ONLY to globals.css:
```css
/* shadcn compatibility — radius token only (components use existing Crowe vars directly) */
:root, html[data-theme="light"], html[data-theme="dark"] {
  --radius: 0.625rem;
}
```

**src/components/ui/Button.tsx**

```typescript
// src/components/ui/Button.tsx
// shadcn-style Button — copy-paste model, no CLI init.
// Uses cn() for class merging. Colors via existing Crowe CSS variables.
// Radix UI not needed for Button (pure HTML button is sufficient).
import { cn } from '@/lib/utils';
import type { ButtonHTMLAttributes } from 'react';

interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'default' | 'outline' | 'ghost' | 'destructive';
  size?: 'sm' | 'md' | 'lg';
}

export function Button({
  className,
  variant = 'default',
  size = 'md',
  disabled,
  ...props
}: ButtonProps) {
  return (
    <button
      disabled={disabled}
      className={cn(
        // Base — layout, font, transition
        'inline-flex items-center justify-center font-semibold transition-all duration-150 cursor-pointer focus-visible:outline-none focus-visible:ring-2 whitespace-nowrap',
        // Disabled state
        disabled && 'opacity-60 cursor-not-allowed',
        // Variants — using Crowe CSS variables via arbitrary values
        variant === 'default' && [
          'bg-[var(--accent)] text-[#011E41] border-0',
          !disabled && 'hover:opacity-90 active:scale-[0.98]',
        ],
        variant === 'outline' && [
          'bg-transparent border border-[var(--border)] text-[var(--foreground)]',
          !disabled && 'hover:bg-[var(--surface)]',
        ],
        variant === 'ghost' && [
          'bg-transparent border-0 text-[var(--foreground)]',
          !disabled && 'hover:bg-[var(--surface)]',
        ],
        variant === 'destructive' && [
          'bg-[#E5376B] text-white border-0',
          !disabled && 'hover:opacity-90',
        ],
        // Sizes
        size === 'sm' && 'text-xs px-3 py-1.5 rounded-md gap-1.5',
        size === 'md' && 'text-sm px-4 py-2 rounded-lg gap-2',
        size === 'lg' && 'text-base px-6 py-3 rounded-xl gap-2',
        className
      )}
      {...props}
    />
  );
}
```

**src/components/ui/Select.tsx**

A thin wrapper over @radix-ui/react-select that applies Crowe CSS variable styling. The API is intentionally similar to shadcn's Select API for future compatibility.

```typescript
// src/components/ui/Select.tsx
// shadcn-style Select — wraps @radix-ui/react-select with Crowe theming.
// Copy-paste model: no shadcn CLI. Radix primitives already installed.
import * as SelectPrimitive from '@radix-ui/react-select';
import { cn } from '@/lib/utils';
import type { ReactNode } from 'react';

// Re-export Root, Value, and Group unchanged — callers use as Select.Root etc.
export const Select = SelectPrimitive.Root;
export const SelectValue = SelectPrimitive.Value;
export const SelectGroup = SelectPrimitive.Group;

// Styled Trigger
interface SelectTriggerProps {
  className?: string;
  children: ReactNode;
  placeholder?: string;
}

export function SelectTrigger({ className, children, ...props }: SelectPrimitive.SelectTriggerProps) {
  return (
    <SelectPrimitive.Trigger
      className={cn(
        'flex items-center justify-between w-full px-3 py-2 text-sm',
        'bg-[var(--card)] border border-[var(--border)] rounded-lg',
        'text-[var(--foreground)] cursor-pointer',
        'hover:bg-[var(--surface)] transition-colors duration-150',
        'focus:outline-none focus:ring-2 focus:ring-[var(--accent)] focus:ring-offset-1',
        'data-[placeholder]:text-[var(--muted-color)]',
        '[&>svg]:ml-auto [&>svg]:opacity-60 [&>svg]:h-4 [&>svg]:w-4',
        className
      )}
      {...props}
    >
      {children}
      <SelectPrimitive.Icon asChild>
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
          <path d="M6 9l6 6 6-6" />
        </svg>
      </SelectPrimitive.Icon>
    </SelectPrimitive.Trigger>
  );
}

// Styled Content (dropdown panel)
export function SelectContent({ className, children, ...props }: SelectPrimitive.SelectContentProps) {
  return (
    <SelectPrimitive.Portal>
      <SelectPrimitive.Content
        position="popper"
        sideOffset={4}
        className={cn(
          'relative z-50 min-w-[var(--radix-select-trigger-width)] overflow-hidden rounded-lg',
          'bg-[var(--card)] border border-[var(--border)]',
          'shadow-[0_4px_16px_rgba(1,30,65,0.12)]',
          'data-[state=open]:animate-in data-[state=closed]:animate-out',
          'data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0',
          'data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95',
          className
        )}
        {...props}
      >
        <SelectPrimitive.Viewport className="p-1">
          {children}
        </SelectPrimitive.Viewport>
      </SelectPrimitive.Content>
    </SelectPrimitive.Portal>
  );
}

// Styled Item
export function SelectItem({ className, children, ...props }: SelectPrimitive.SelectItemProps) {
  return (
    <SelectPrimitive.Item
      className={cn(
        'relative flex items-center px-3 py-2 text-sm rounded-md cursor-pointer select-none',
        'text-[var(--foreground)] outline-none',
        'data-[highlighted]:bg-[var(--accent-soft)] data-[highlighted]:text-[var(--foreground)]',
        'data-[state=checked]:font-semibold',
        className
      )}
      {...props}
    >
      <SelectPrimitive.ItemText>{children}</SelectPrimitive.ItemText>
    </SelectPrimitive.Item>
  );
}

// Styled Label (optional, for group headers)
export function SelectLabel({ className, ...props }: SelectPrimitive.SelectLabelProps) {
  return (
    <SelectPrimitive.Label
      className={cn('px-3 py-1.5 text-xs font-semibold text-[var(--muted-color)] uppercase tracking-wide', className)}
      {...props}
    />
  );
}

// Separator
export function SelectSeparator({ className, ...props }: SelectPrimitive.SelectSeparatorProps) {
  return (
    <SelectPrimitive.Separator
      className={cn('my-1 h-px bg-[var(--border)]', className)}
      {...props}
    />
  );
}
```

**src/components/ui/Tooltip.tsx**

```typescript
// src/components/ui/Tooltip.tsx
// shadcn-style Tooltip — wraps @radix-ui/react-tooltip with Crowe theming.
// TooltipProvider must wrap the app (or section). Add to DashboardApp in Plan 10-05.
import * as TooltipPrimitive from '@radix-ui/react-tooltip';
import { cn } from '@/lib/utils';

export const TooltipProvider = TooltipPrimitive.Provider;
export const Tooltip = TooltipPrimitive.Root;
export const TooltipTrigger = TooltipPrimitive.Trigger;

export function TooltipContent({
  className,
  sideOffset = 6,
  ...props
}: TooltipPrimitive.TooltipContentProps) {
  return (
    <TooltipPrimitive.Portal>
      <TooltipPrimitive.Content
        sideOffset={sideOffset}
        className={cn(
          'z-50 max-w-[220px] rounded-lg px-3 py-2 text-xs',
          'bg-[var(--card)] border border-[var(--border)]',
          'text-[var(--foreground)]',
          'shadow-[0_4px_16px_rgba(1,30,65,0.12)]',
          'animate-in fade-in-0 zoom-in-95',
          'data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=closed]:zoom-out-95',
          className
        )}
        {...props}
      />
    </TooltipPrimitive.Portal>
  );
}
```

After creating all files, run Vitest:
```bash
node "Catie/FP&A Application/fpa-close-efficiency-dashboard/node_modules/vitest/vitest.mjs" run --reporter=verbose
```
  </action>
  <verify>
    <automated>node "Catie/FP&A Application/fpa-close-efficiency-dashboard/node_modules/vitest/vitest.mjs" run --reporter=verbose</automated>
  </verify>
  <done>Button.tsx, Select.tsx, Tooltip.tsx created in src/components/ui/. globals.css has --radius token. Vitest 86/86 GREEN. TypeScript clean on new files.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hand-crafted buttons/dropdown in AiSummarySection, ScenarioPanel, and add KpiCard Tooltip</name>
  <files>
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/AiSummarySection/AiSummarySection.tsx
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/ScenarioPanel/ScenarioPanel.tsx
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/KpiCard.tsx
  </files>
  <action>
Make targeted replacements in 3 component files. Minimal changes — replace only the specific elements identified in CONTEXT.md. All existing logic (Redux dispatch, streaming, sentinel option) remains unchanged.

**AiSummarySection.tsx — Replace generate/regenerate button**

Find the existing `<button onClick={handleGenerate} ...>` block and replace with the shadcn Button component:

1. Add import: `import { Button } from '@/components/ui/Button';`
2. Replace the hand-crafted button with:

```typescript
<Button
  onClick={handleGenerate}
  disabled={isStreaming}
  variant="default"
  size="sm"
  style={{ opacity: isStreaming ? 0.6 : 1 }}
>
  {isStreaming ? 'Generating\u2026' : buttonLabel}
</Button>
```

The Button component handles all styling via cn(). The variant="default" gives the Crowe amber background (#F5A800). Remove the hand-crafted inline style block entirely.

**ScenarioPanel.tsx — Replace SelectPrimitive with shadcn Select, Replace Reset button**

This is the most complex replacement. The existing code uses `* as SelectPrimitive from '@radix-ui/react-select'` directly with full inline styling. Replace with the shadcn Select components.

1. Remove `import * as SelectPrimitive from '@radix-ui/react-select';` (it's re-exported through Select.tsx)
2. Add imports:
```typescript
import { Select, SelectTrigger, SelectContent, SelectItem, SelectValue } from '@/components/ui/Select';
import { Button } from '@/components/ui/Button';
```

3. Find the PresetRow component (or wherever the Radix Select is rendered inline) and replace all `<SelectPrimitive.Root>`, `<SelectPrimitive.Trigger>`, `<SelectPrimitive.Content>`, `<SelectPrimitive.Item>`, `<SelectPrimitive.Value>` with the corresponding named exports.

The existing onValueChange handler and sentinel option (`value="__custom__"`) MUST be preserved exactly — only the JSX element names change, not the logic.

Example replacement pattern:
```typescript
// BEFORE:
<SelectPrimitive.Root value={activePresetId ?? '__custom__'} onValueChange={handlePresetChange}>
  <SelectPrimitive.Trigger style={{...}}>
    <SelectPrimitive.Value placeholder="Select preset" />
  </SelectPrimitive.Trigger>
  <SelectPrimitive.Portal>
    <SelectPrimitive.Content position="popper" style={{...}}>
      <SelectPrimitive.Viewport>
        <SelectPrimitive.Item value="__custom__" style={{...}}>— Custom —</SelectPrimitive.Item>
        {presets.map(p => (
          <SelectPrimitive.Item key={p.id} value={p.id} style={{...}}>{p.label}</SelectPrimitive.Item>
        ))}
      </SelectPrimitive.Viewport>
    </SelectPrimitive.Content>
  </SelectPrimitive.Portal>
</SelectPrimitive.Root>

// AFTER:
<Select value={activePresetId ?? '__custom__'} onValueChange={handlePresetChange}>
  <SelectTrigger style={{ minWidth: '180px' }}>
    <SelectValue placeholder="Select preset" />
  </SelectTrigger>
  <SelectContent>
    <SelectItem value="__custom__">— Custom —</SelectItem>
    {presets.map(p => (
      <SelectItem key={p.id} value={p.id}>{p.label}</SelectItem>
    ))}
  </SelectContent>
</Select>
```

4. Find the Reset button in ScenarioPanel and replace the hand-crafted `<button onClick={handleReset}>Reset to defaults</button>` with:
```typescript
<Button onClick={handleReset} variant="outline" size="sm">
  Reset to defaults
</Button>
```

**KpiCard.tsx — Add Tooltip around the label area**

Read the current KpiCard.tsx first to understand the existing structure, then add:

1. Add imports:
```typescript
import { Tooltip, TooltipTrigger, TooltipContent } from '@/components/ui/Tooltip';
```

2. The KpiCard receives a `label` prop. Add a brief tooltip description per label. Since the tooltip text is generic enough to compute from the label, use a simple inline mapping or pass as an optional prop. The simplest approach that requires no prop API change: use a lookup object inside KpiCard with fallback to the label itself.

```typescript
const KPI_DESCRIPTIONS: Record<string, string> = {
  'Net Sales': 'Revenue after returns and discounts for the period',
  'Gross Profit': 'Net Sales minus Cost of Goods Sold',
  'EBITDA': 'Earnings before interest, taxes, depreciation, and amortization',
  'Cash': '13-week cash position from cash flow statement',
  'COGS': 'Cost of Goods Sold — decreases indicate margin improvement',
  'Accounts Receivable': 'Outstanding customer invoices — aging tracked separately',
  'Accounts Payable': 'Outstanding vendor invoices due for payment',
  'Inventory': 'On-hand inventory value at period end',
};
```

Wrap the existing label `<span>` or text with:
```typescript
<Tooltip>
  <TooltipTrigger asChild>
    <span style={{ cursor: 'help', /* existing label styles */ }}>
      {label}
    </span>
  </TooltipTrigger>
  <TooltipContent>
    {KPI_DESCRIPTIONS[label] ?? label}
  </TooltipContent>
</Tooltip>
```

NOTE: TooltipProvider must be somewhere higher in the tree. Since DashboardApp is the root client component, add `<TooltipProvider>` wrapping inside DashboardApp.tsx. This is a small amendment to DashboardApp — add the import and wrap the Provider tree:

```typescript
// In DashboardApp.tsx — add after other imports:
import { TooltipProvider } from '@/components/ui/Tooltip';

// In JSX — wrap Provider:
<Provider store={storeRef.current}>
  <TooltipProvider delayDuration={300}>
    {/* existing layout */}
  </TooltipProvider>
</Provider>
```

After all replacements, run Vitest:
```bash
node "Catie/FP&A Application/fpa-close-efficiency-dashboard/node_modules/vitest/vitest.mjs" run --reporter=verbose
```
Expected: 86/86 GREEN. The Redux scenario slice tests are unaffected because we only changed JSX wrappers, not dispatch logic.
  </action>
  <verify>
    <automated>node "Catie/FP&A Application/fpa-close-efficiency-dashboard/node_modules/vitest/vitest.mjs" run --reporter=verbose</automated>
  </verify>
  <done>
    AiSummarySection uses Button component for Generate/Regenerate.
    ScenarioPanel uses Select/SelectTrigger/SelectContent/SelectItem for preset dropdown (all 6 presets + sentinel option work).
    ScenarioPanel uses Button for Reset to defaults.
    KpiCard wraps label in Tooltip with KPI_DESCRIPTIONS.
    DashboardApp wraps with TooltipProvider.
    Vitest 86/86 GREEN. TypeScript clean.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `src/components/ui/Button.tsx` exports `Button`
2. `src/components/ui/Select.tsx` exports `Select, SelectTrigger, SelectContent, SelectItem, SelectValue`
3. `src/components/ui/Tooltip.tsx` exports `Tooltip, TooltipTrigger, TooltipContent, TooltipProvider`
4. `AiSummarySection.tsx` imports and uses `Button` (no hand-crafted button for generate/regenerate)
5. `ScenarioPanel.tsx` imports and uses `Select` family (no `SelectPrimitive` direct usage)
6. `ScenarioPanel.tsx` imports and uses `Button` for reset
7. `KpiCard.tsx` imports and uses `Tooltip` family
8. `DashboardApp.tsx` wraps with `TooltipProvider`
9. `globals.css` has `--radius: 0.625rem` (no existing variables overwritten)
10. Vitest: 86/86 GREEN
11. TypeScript: clean
</verification>

<success_criteria>
- shadcn Button component used for Generate/Regenerate, Reset, and (in next plan) landing CTA
- shadcn Select replaces the Radix Select direct usage in ScenarioPanel
- shadcn Tooltip shows on KPI card label hover
- Both themes (light/dark) render all components correctly
- VISU-03 requirement met
</success_criteria>

<output>
After completion, create `.planning/phases/10-visual-identity-and-interactivity/10-04-SUMMARY.md`
</output>
