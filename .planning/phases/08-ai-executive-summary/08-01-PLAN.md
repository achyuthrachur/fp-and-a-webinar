---
phase: 08-ai-executive-summary
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/features/model/__tests__/aiSummary.test.ts
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/app/api/enhance-summary/route.ts
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/lib/aiSummaryCache.ts
autonomous: true
requirements:
  - AISU-01
  - AISU-04

must_haves:
  truths:
    - "Running the Vitest suite produces FAILED (not SKIPPED) results for all aiSummary.test.ts assertions"
    - "route.ts stub file exists and exports buildUserPrompt as a named export"
    - "aiSummaryCache.ts stub file exists and exports BASELINE_SUMMARY as a named export"
  artifacts:
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/features/model/__tests__/aiSummary.test.ts"
      provides: "RED test stubs for buildUserPrompt and BASELINE_SUMMARY"
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/app/api/enhance-summary/route.ts"
      provides: "Stub route handler exporting buildUserPrompt"
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/lib/aiSummaryCache.ts"
      provides: "Stub cache module exporting BASELINE_SUMMARY"
  key_links:
    - from: "aiSummary.test.ts"
      to: "route.ts"
      via: "dynamic import @/app/api/enhance-summary/route"
      pattern: "import.*enhance-summary/route"
    - from: "aiSummary.test.ts"
      to: "aiSummaryCache.ts"
      via: "dynamic import @/lib/aiSummaryCache"
      pattern: "import.*aiSummaryCache"
---

<objective>
Create the Wave 0 RED test stubs for Phase 8, plus the minimal stub files that satisfy the imports. Tests must FAIL (not skip) to confirm the RED phase of TDD.

Purpose: Establish the test contract for buildUserPrompt (AISU-01) and BASELINE_SUMMARY (AISU-04) before implementation. Gives Wave 1 a clear GREEN target.
Output: Three files — test stubs that fail, route.ts stub with empty buildUserPrompt, aiSummaryCache.ts stub with empty BASELINE_SUMMARY.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ai-executive-summary/08-CONTEXT.md
@.planning/phases/08-ai-executive-summary/08-RESEARCH.md
@.planning/phases/08-ai-executive-summary/08-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stub route.ts and aiSummaryCache.ts</name>
  <files>
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/app/api/enhance-summary/route.ts
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/lib/aiSummaryCache.ts
  </files>
  <action>
Create the directory `src/app/api/enhance-summary/` and write `route.ts` as a minimal stub that satisfies TypeScript and exports `buildUserPrompt` as a named export. The function body returns an empty string — this is intentionally wrong so tests fail in Wave 0.

`route.ts` contents:
```typescript
// src/app/api/enhance-summary/route.ts
// STUB — Wave 0 placeholder. Full implementation in Plan 08-02.
export const runtime = 'nodejs';

import { NextRequest } from 'next/server';

export interface KpiPayload {
  netSales: string;
  cogs: string;
  grossProfit: string;
  ebitda: string;
  cash: string;
  ar: string;
  ap: string;
  inventory: string;
}

// Pure function exported for testability — Wave 0 stub returns empty string.
export function buildUserPrompt(kpis: KpiPayload, presetName: string): string {
  return '';
}

export async function POST(_req: NextRequest) {
  return new Response('stub', { status: 200 });
}
```

`aiSummaryCache.ts` contents:
```typescript
// src/lib/aiSummaryCache.ts
// STUB — Wave 0 placeholder. Full content in Plan 08-02.
export const BASELINE_SUMMARY: string = '';
```
  </action>
  <verify>
    <automated>node "Catie/FP&A Application/fpa-close-efficiency-dashboard/node_modules/vitest/vitest.mjs" run --reporter=verbose 2>&1 | head -30</automated>
  </verify>
  <done>Both stub files exist. TypeScript can resolve the imports. No "Cannot find module" errors in Vitest output.</done>
</task>

<task type="auto">
  <name>Task 2: Write RED test stubs in aiSummary.test.ts</name>
  <files>
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/features/model/__tests__/aiSummary.test.ts
  </files>
  <action>
Write the test file using the established `beforeAll` error-capture pattern. Two separate `beforeAll` blocks — one for the route module (buildUserPrompt), one for the cache module (BASELINE_SUMMARY) — each catches import errors independently so failures in one do not mask the other.

Tests must FAIL with assertion errors (not SKIPPED). The stub implementations in Task 1 return empty strings, so all assertions below are intentionally unsatisfied in Wave 0.

Write this exact file:

```typescript
// src/features/model/__tests__/aiSummary.test.ts
// Wave 0 RED stubs — all tests must FAIL until Plan 08-02 implements the real code.
import { describe, it, expect, beforeAll } from 'vitest';
import type { KpiPayload } from '@/app/api/enhance-summary/route';

let buildUserPrompt: (kpis: KpiPayload, presetName: string) => string;
let BASELINE_SUMMARY: string;
let routeImportError: unknown;
let cacheImportError: unknown;

beforeAll(async () => {
  try {
    const routeMod = await import('@/app/api/enhance-summary/route');
    buildUserPrompt = (routeMod as unknown as Record<string, unknown>).buildUserPrompt as typeof buildUserPrompt;
  } catch (err) {
    routeImportError = err;
  }
});

beforeAll(async () => {
  try {
    const cacheMod = await import('@/lib/aiSummaryCache');
    BASELINE_SUMMARY = (cacheMod as unknown as Record<string, unknown>).BASELINE_SUMMARY as string;
  } catch (err) {
    cacheImportError = err;
  }
});

describe('buildUserPrompt (AISU-01)', () => {
  it('Test 1: includes all 8 KPI labels in output', () => {
    if (routeImportError) throw routeImportError;
    const kpis: KpiPayload = {
      netSales: '$9,476,000',
      cogs: '$7,337,267',
      grossProfit: '$2,138,733',
      ebitda: '$958,733',
      cash: '$4,250,000',
      ar: '$2,800,000',
      ap: '$3,100,000',
      inventory: '$6,400,000',
    };
    const result = buildUserPrompt(kpis, 'Jan 2026 Baseline');
    expect(result).toContain('Net Sales');
    expect(result).toContain('COGS');
    expect(result).toContain('Gross Profit');
    expect(result).toContain('EBITDA');
    expect(result).toContain('Cash');
    expect(result).toContain('Accounts Receivable');
    expect(result).toContain('Accounts Payable');
    expect(result).toContain('Inventory');
  });

  it('Test 2: includes presetName in output', () => {
    if (routeImportError) throw routeImportError;
    const kpis: KpiPayload = {
      netSales: '$9,476,000',
      cogs: '$7,337,267',
      grossProfit: '$2,138,733',
      ebitda: '$958,733',
      cash: '$4,250,000',
      ar: '$2,800,000',
      ap: '$3,100,000',
      inventory: '$6,400,000',
    };
    const result = buildUserPrompt(kpis, 'Fuel Cost Shock');
    expect(result).toContain('Fuel Cost Shock');
  });

  it('Test 3: includes the instruction to write the summary', () => {
    if (routeImportError) throw routeImportError;
    const kpis: KpiPayload = {
      netSales: '$9,476,000',
      cogs: '$7,337,267',
      grossProfit: '$2,138,733',
      ebitda: '$958,733',
      cash: '$4,250,000',
      ar: '$2,800,000',
      ap: '$3,100,000',
      inventory: '$6,400,000',
    };
    const result = buildUserPrompt(kpis, 'Jan 2026 Baseline');
    expect(result).toContain('executive summary');
  });
});

describe('BASELINE_SUMMARY (AISU-04)', () => {
  it('Test 4: exports a non-empty string', () => {
    if (cacheImportError) throw cacheImportError;
    expect(typeof BASELINE_SUMMARY).toBe('string');
    expect(BASELINE_SUMMARY.length).toBeGreaterThan(100);
  });

  it('Test 5: contains CFO-relevant financial reference ($9.5M area)', () => {
    if (cacheImportError) throw cacheImportError;
    const hasFinancialContent =
      BASELINE_SUMMARY.includes('9.5') ||
      BASELINE_SUMMARY.includes('$9') ||
      BASELINE_SUMMARY.includes('959');
    expect(hasFinancialContent).toBe(true);
  });

  it('Test 6: contains two paragraphs (double newline separator)', () => {
    if (cacheImportError) throw cacheImportError;
    expect(BASELINE_SUMMARY).toContain('\n\n');
  });
});
```
  </action>
  <verify>
    <automated>node "Catie/FP&A Application/fpa-close-efficiency-dashboard/node_modules/vitest/vitest.mjs" run --reporter=verbose 2>&1 | grep -E "(FAIL|PASS|aiSummary)"</automated>
  </verify>
  <done>
    - aiSummary.test.ts exists and is picked up by Vitest (appears in output).
    - All 6 tests in the file show as FAILED (red), not SKIPPED.
    - No "Cannot find module" errors — the beforeAll error-capture pattern catches import issues correctly and re-throws them inside each it().
    - All other existing tests remain GREEN (this file does not break the broader suite, it only adds new failing tests).
  </done>
</task>

</tasks>

<verification>
Run the full Vitest suite and confirm:
1. aiSummary.test.ts appears in output with 6 FAILED tests
2. No new failures in any other test file — only aiSummary.test.ts is red
3. route.ts and aiSummaryCache.ts are importable (no "Cannot find module" in Vitest stderr)
</verification>

<success_criteria>
- 6 new test cases in aiSummary.test.ts — all FAILED
- Existing test suite still passes for all non-Phase-8 files
- route.ts exports both `buildUserPrompt` (named) and `POST` (named)
- aiSummaryCache.ts exports `BASELINE_SUMMARY` (named, empty string stub)
- RED phase of TDD is complete — Wave 1 has clear targets to turn GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-executive-summary/08-01-SUMMARY.md`
</output>
